# ============================================================
# AI Job Search Assistant — Python 3.12+ compatible
# Install: pip install -r requirements.txt
# ============================================================

# --- Core framework ---
pydantic==2.7.1              # Data validation and settings management
pydantic-settings==2.3.0     # .env file loading into typed settings

# --- Configuration & environment ---
python-dotenv==1.0.1         # Load .env files into os.environ

# --- Logging ---
loguru==0.7.2                # Structured, colourful logging with rotation

# --- HTTP client (for API calls and scraping) ---
httpx==0.27.0                # Async-first HTTP client, replaces requests
tenacity==8.3.0              # Retry logic with exponential backoff

# --- HTML parsing (for job board scraping) ---
beautifulsoup4==4.12.3       # HTML parsing
lxml==5.2.2                  # Fast XML/HTML parser backend for bs4

# --- Browser automation (Playwright, not Selenium) ---
playwright==1.44.0           # Modern browser automation, async support

# --- AI / LLM ---
anthropic==0.28.0            # Claude API client (cover letters, messages)
openai==1.30.1               # Optional: GPT fallback

# --- Job data storage ---
tinydb==4.8.0                # Lightweight local JSON database
                             # No server needed — perfect for local execution

# --- Scheduling (daily plan runner) ---
schedule==1.2.2              # Simple job scheduling for daily automation
apscheduler==3.10.4          # Advanced scheduler with cron-like syntax

# --- Email (Gmail integration) ---
google-auth==2.29.0          # Google OAuth2
google-auth-oauthlib==1.2.0  # OAuth flow for Gmail
google-api-python-client==2.131.0  # Gmail API client

# --- LinkedIn (via Playwright, no official API) ---
# playwright handles LinkedIn — no extra lib needed

# --- Data handling ---
pandas==2.2.2                # Data manipulation for job deduplication
python-dateutil==2.9.0       # Date parsing for application tracking

# --- CLI interface ---
rich==13.7.1                 # Beautiful terminal output, tables, progress bars
typer==0.12.3                # Clean CLI argument parsing

# --- Utilities ---
python-slugify==8.0.4        # URL-safe slugs for job IDs
jinja2==3.1.4                # Templating for cover letters and messages

# --- Testing ---
pytest==8.2.2                # Test runner
pytest-asyncio==0.23.7       # Async test support
pytest-cov==5.0.0            # Coverage reports
